package migrate

const (
	// DefaultMigrationsTableName is the default table used to persist migration history.
	DefaultMigrationsTableName = "migrations"
	// DefaultMigrationsDir is the default filesystem location for SQL migration files.
	DefaultMigrationsDir = "./migrations"
)
